import type { Spec } from "./NativeVisualPositioningSystem";

console.error(
  "react-native-visual-positioning-system is not linked. Please run `npx react-native link react-native-visual-positioning-system` to link the native module."
);
const mock = null;
export default mock as any as Spec | null;
